[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && ./node_modules/.bin/tsc && cd ../frontend && npm install && npm run build"

[deploy]
startCommand = "cd backend && PORT=3001 node dist/index.js & cd frontend && npm start"
